import time

# large_list = [i for i in range(38347922)]
# this number is estimated to use ~1GB of memory

large_list = [i for i in range(19173961)]
# this number is estimated to use ~0.5GB of memory

time.sleep(600)
